# Docker

총 15개 질문입니다.

## Docker

~~38. Docker 컨테이너는 가상머신과 무엇이 다를까?~~ `작성완료`
~~39. Docker 이미지는 하나의 파일일까, 여러 레이어의 조합일까?~~ `작성완료`
~~40. `Dockerfile`의 명령어 순서가 빌드 속도와 캐시에 왜 영향을 줄까?~~ `작성완료`
~~41. 컨테이너 안에서 파일을 만들면 컨테이너를 지운 뒤에도 남아 있을까?~~ `작성완료`
~~42. Docker volume은 bind mount와 어떤 차이가 있을까?~~ `작성완료`
~~43. 컨테이너는 기본적으로 서로 통신할 수 있을까, 네트워크를 따로 연결해야 할까?~~ `작성완료`
~~44. `docker compose`는 단순히 여러 컨테이너를 한 번에 실행하는 도구일까?~~ `작성완료`
~~88. Docker 이미지에서 base image를 고르는 기준은 용량뿐일까, 보안과 디버깅 편의성도 포함될까?~~ `작성완료`
~~89. multi-stage build는 이미지 크기만 줄이는 기법일까, 빌드 산출물의 경계를 분리하는 기법일까?~~ `작성완료`
90. 컨테이너를 root 사용자로 실행하면 어떤 보안 위험이 생길까?
91. Docker healthcheck는 애플리케이션 내부의 readiness를 정확히 보장할 수 있을까?
92. 컨테이너 로그를 파일에 직접 쓰는 방식과 stdout/stderr로 보내는 방식은 운영에서 어떤 차이가 있을까?
93. 컨테이너의 CPU, memory limit을 설정하지 않으면 호스트와 다른 컨테이너에 어떤 영향을 줄 수 있을까?
## Network / Infrastructure

405. container image supply chain 보안은 base image, dependency scan, signing, provenance를 어떻게 포함해야 할까?
406. immutable infrastructure와 mutable server 운영 방식은 장애 복구와 drift 관리에서 어떻게 다를까?
