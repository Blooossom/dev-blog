# Computer Science

총 17개 질문입니다.

## Computer Science

~~118. 프로세스와 스레드는 메모리 공간, 문맥 전환 비용, 장애 격리 측면에서 어떻게 다를까?~~ `작성완료`
~~119. 커널 모드와 유저 모드가 분리되어 있는 이유는 단순한 권한 구분 때문일까?~~ `작성완료`
~~120. 시스템 콜은 일반 함수 호출과 비교했을 때 왜 더 비싼 작업일까?~~ `작성완료`
~~121. 문맥 전환이 잦아지면 CPU를 많이 쓰지 않는 프로그램도 느려질 수 있는 이유는 무엇일까?~~ `작성완료`
~~122. CPU 캐시 지역성은 자료구조 선택과 반복문 성능에 어떤 영향을 줄까?~~ `작성완료`
~~123. 가상 메모리는 실제 메모리보다 큰 공간을 쓰게 해주는 기능일까, 프로세스 격리와 주소 변환을 위한 추상화일까?~~ `작성완료`
124. page fault는 항상 나쁜 현상일까, minor fault와 major fault는 무엇이 다를까?
~~125. 메모리 누수는 GC가 있는 언어에서도 왜 발생할 수 있을까?~~ `작성완료`
~~126. race condition과 deadlock은 둘 다 동시성 버그지만, 원인과 해결 방식이 어떻게 다를까?~~ `작성완료`
~~127. deadlock의 4가지 필요 조건 중 하나를 깨는 방식은 실제 시스템 설계에서 어떻게 적용될 수 있을까?~~ `작성완료`
128. lock-free 자료구조는 lock을 쓰지 않으니 항상 더 빠르다고 말할 수 있을까?
~~129. CAP 이론은 분산 시스템에서 항상 C, A, P 중 두 개만 고르라는 뜻일까?~~ `작성완료`
130. eventual consistency는 단순히 데이터가 늦게 맞춰진다는 뜻일까, 사용자 경험과 보상 로직까지 포함하는 설계 문제일까?
131. idempotency는 결제, 주문, 메시지 처리 같은 시스템에서 왜 중요한가?
132. 메시지 큐에서 at-most-once, at-least-once, exactly-once는 실제로 어떤 실패 시나리오를 다르게 처리할까?
133. 분산 락은 단일 DB 락보다 항상 더 좋은 선택일까?
~~134. 캐시를 도입하면 성능은 좋아지지만, cache stampede와 stale data 문제는 어떻게 다뤄야 할까?~~ `작성완료`
